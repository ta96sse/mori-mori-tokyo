# Mori-Mori TOKYO — Claude Code設定

プロジェクト詳細・ルール → AGENTS.md（作業開始前に必ず読むこと）

## 役割
- コード実装・型チェック・ビルド確認を単体で完結する
- `yarn` のみ使用（npm/npx禁止）
- `yarn astro check` を修正ループで活用
- マルチエージェント協調が必要な場合は AGENTS.md のプロトコルに従う
